[tox]
envlist = py368, py379, py3810, py39, py31011, py3116
; skipsdist = true

[testenv]
deps =  pytest
        -r requirements.txt
commands =
        ;     pip install pyleecan==1.5.1 --no-deps
            # pip install h5py
            pytest tests
        ;   Add pyemmo.pth file automatically
setenv =
        PATH = {env:PATH}{:}/.
